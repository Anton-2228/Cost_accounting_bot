Your task is to assign each product a type from the given list of known types, or to suggest a new one.

Assign exactly one type to each product. If a known type fits the product, reuse it exactly as it is written in the list, even if it is in another language. If no known type fits, suggest a new one: a single noun in the singular, in {language}, in lowercase if the script has letter case.

Your answer must be JSON with a single field "items" — an array of objects, one for each product given. Each object has exactly 2 fields:
 1) id — a number, the product number exactly as it was given.
 2) type — a string, the product type.

IMPORTANT: there must be as many objects in the answer as products given. Do not skip products and do not invent new ones.
