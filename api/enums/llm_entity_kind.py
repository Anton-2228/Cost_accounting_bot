"""Вид сущности, к которой относится обращение к модели."""

from __future__ import annotations

from enum import StrEnum


class LlmEntityKind(StrEnum):
    """К строке какой таблицы привязан замер.

    Пара «вид + идентификатор» намеренно не является внешним ключом: вид здесь
    ровно один, но обращения к модели появляются раньше и живут дольше, чем
    сущности, о которых они спрашивают. Разбор чека зовёт модель до того, как
    возникла хоть одна операция реестра, и одно обращение покрывает сразу все
    позиции чека — ссылка на `records` была бы неверной по кратности, а не
    просто неудобной.

    Отсюда же необязательность пары: обращение вне какой-либо сущности
    выразимо, и заводить ради него отдельное значение вида не придётся.
    """

    CHECK = "CHECK"
