"""Зависимости FastAPI: сессия, репозитории, сервисы.

Всё строится от одной зависимости `get_session`. FastAPI кэширует результат
зависимости в пределах запроса, поэтому репозитории и сервис получают **одну и ту
же** `AsyncSession` — а значит и одну транзакцию, которую сервис завершает одним
коммитом.
"""

from __future__ import annotations
