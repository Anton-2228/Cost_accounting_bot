"""Бэкенд Telegram Mini App, добавляющего чеки по QR-коду.

Отдельный сервис, а не эндпоинт в api: api закрыт на `127.0.0.1` и не делает ни
одного внешнего вызова — оба свойства записаны как инварианты. Mini App же
обязан быть доступен из интернета (иначе Telegram не откроет страницу) и обязан
ходить во внешний сервис расшифровки чеков. Совмещать это в api значило бы
отказаться от обоих инвариантов сразу.

Своей базы у сервиса нет: всё состояние живёт в api и берётся по HTTP. Импорт
пакета не имеет побочных эффектов, кроме чтения настроек в
:mod:`checks_service.config`.
"""

from __future__ import annotations
