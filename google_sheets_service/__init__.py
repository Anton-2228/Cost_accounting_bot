"""Сервис, разбирающий очередь `sheet_sync_tasks` и работающий с Google API.

Единственная часть системы, которая ходит в Google. Собственной базы у него нет:
всё состояние — в api, откуда оно и берётся по HTTP. Импорт пакета не имеет
побочных эффектов, кроме чтения настроек в :mod:`google_sheets_service.config`.
"""

from __future__ import annotations
